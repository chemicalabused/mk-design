# mk-design

Landing page / portfolio projektanta. Vite + React (TypeScript) + Tailwind CSS v4.

```
npm install
npm run dev
```

Pozostałe komendy: `npm run build`, `npm run lint`, `npm run preview`.
